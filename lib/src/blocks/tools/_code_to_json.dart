import 'package:taichi/src/blocks/_enums.dart';

import '../_block_controller.dart';

/// 生成 ```json``` 格式的组件样式，
///
/// 然后可以保存下来之后继续使用

Map<String, dynamic> code2json(BlockController controller) {
  Map<String, dynamic> res = {};

  if (controller.boardType == BoardType.stack) {}

  return res;
}
