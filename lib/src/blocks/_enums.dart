/// ```stack``` 固定位置
///
/// ```scrollerView``` 可滚动的组件
///
/// ```common``` 不可滚动，非固定位置自适应
enum BoardType { stack, scrollView, common }

/// ```ltr``` 从左往右
///
/// ```ttb``` 从上到下
enum BoardDirection { ltr, ttb }
