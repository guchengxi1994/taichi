import '_enums.dart';
import '_drag_controller.dart';

String codeGenerator(BlockController controller) {
  if (controller.boardType == BoardType.stack) {
    for (int i = 0; i < controller.widgets.length; i++) {}
  }

  return "";
}
