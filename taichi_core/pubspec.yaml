name: taichi
description: A(n) (combinition of) interesting UI widget(s) and tool(s).

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.7+3 # dev7

homepage: https://github.com/guchengxi1994/taichi

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.3
  dart_date: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1


flutter:
  uses-material-design: true


