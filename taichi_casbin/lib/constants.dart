/// perm symbols
///
/// equals  ```==```
///
/// and   ```&&```
///
/// or   ```||```
enum Symbols { equals, and, or }

/// perm keys
///
/// some ```some```
///
/// where ```where```
enum Keywords { some, where }
